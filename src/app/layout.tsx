import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Prompt Architect - AI System Prompt Generator",
  description: "Transform vague ideas into structured, professional-grade AI system prompts through a guided multi-stage questionnaire. Generate technical blueprints for software architecture and development.",
  keywords: ["AI", "prompt engineering", "system prompts", "software architecture", "technical planning"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <div className="noise" />
      </body>
    </html>
  );
}
